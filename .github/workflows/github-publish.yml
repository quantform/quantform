name: npm

on:
  push:
    branches:
      - main
    tags:
      - v*

jobs:
  core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
          registry-url: 'https://npm.pkg.github.com'

      - run: npm install
        working-directory: ./packages/core
      - run: npm run test:unit
        working-directory: ./packages/core
      - run: npm version 0.1.${{ github.run_number }} --no-git-tag-version
        working-directory: ./packages/core
      - run: npm publish
        working-directory: ./packages/core
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  sqlite:
    runs-on: ubuntu-latest
    needs: core
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
          registry-url: 'https://npm.pkg.github.com'

      - run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.QUANTFORM_SECRET }}" >> .npmrc
        working-directory: ./packages/feed/sqlite
      - run: npm install @quantform/core@0.1.${{ github.run_number }}
        working-directory: ./packages/feed/sqlite
      - run: npm install
        working-directory: ./packages/feed/sqlite
      - run: npm run test:unit
        working-directory: ./packages/feed/sqlite
      - run: npm version 0.1.${{ github.run_number }} --no-git-tag-version
        working-directory: ./packages/feed/sqlite
      - run: npm publish
        working-directory: ./packages/feed/sqlite
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  binance:
    runs-on: ubuntu-latest
    needs: core
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
          registry-url: 'https://npm.pkg.github.com'

      - run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.QUANTFORM_SECRET }}" >> .npmrc
        working-directory: ./packages/adapter/binance
      - run: npm install @quantform/core@0.1.${{ github.run_number }}
        working-directory: ./packages/adapter/binance
      - run: npm install
        working-directory: ./packages/adapter/binance
      #- run: npm run test:unit
      #  working-directory: ./packages/exchange-binance
      - run: npm version 0.1.${{ github.run_number }} --no-git-tag-version
        working-directory: ./packages/adapter/binance
      - run: npm publish
        working-directory: ./packages/adapter/binance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  binance-delivery:
    runs-on: ubuntu-latest
    needs: core
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
          registry-url: 'https://npm.pkg.github.com'

      - run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.QUANTFORM_SECRET }}" >> .npmrc
        working-directory: ./packages/adapter/binance-delivery
      - run: npm install @quantform/core@0.1.${{ github.run_number }}
        working-directory: ./packages/adapter/binance-delivery
      - run: npm install
        working-directory: ./packages/adapter/binance-delivery
      #- run: npm run test:unit
      #  working-directory: ./packages/exchange-binance
      - run: npm version 0.1.${{ github.run_number }} --no-git-tag-version
        working-directory: ./packages/adapter/binance-delivery
      - run: npm publish
        working-directory: ./packages/adapter/binance-delivery
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  binance-future:
    runs-on: ubuntu-latest
    needs: core
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
          registry-url: 'https://npm.pkg.github.com'

      - run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.QUANTFORM_SECRET }}" >> .npmrc
        working-directory: ./packages/adapter/binance-future
      - run: npm install @quantform/core@0.1.${{ github.run_number }}
        working-directory: ./packages/adapter/binance-future
      - run: npm install
        working-directory: ./packages/adapter/binance-future
      #- run: npm run test:unit
      #  working-directory: ./packages/exchange-binance
      - run: npm version 0.1.${{ github.run_number }} --no-git-tag-version
        working-directory: ./packages/adapter/binance-future
      - run: npm publish
        working-directory: ./packages/adapter/binance-future
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  dukascopy:
    runs-on: ubuntu-latest
    needs: core
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
          registry-url: 'https://npm.pkg.github.com'

      - run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.QUANTFORM_SECRET }}" >> .npmrc
        working-directory: ./packages/adapter/dukascopy
      - run: npm install @quantform/core@0.1.${{ github.run_number }}
        working-directory: ./packages/adapter/dukascopy
      - run: npm install
        working-directory: ./packages/adapter/dukascopy
      #- run: npm run test:unit
      #  working-directory: ./packages/exchange-binance
      - run: npm version 0.1.${{ github.run_number }} --no-git-tag-version
        working-directory: ./packages/adapter/dukascopy
      - run: npm publish
        working-directory: ./packages/adapter/dukascopy
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  oanda:
    runs-on: ubuntu-latest
    needs: core
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
          registry-url: 'https://npm.pkg.github.com'

      - run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.QUANTFORM_SECRET }}" >> .npmrc
        working-directory: ./packages/adapter/oanda
      - run: npm install @quantform/core@0.1.${{ github.run_number }}
        working-directory: ./packages/adapter/oanda
      - run: npm install
        working-directory: ./packages/adapter/oanda
      #- run: npm run test:unit
      #  working-directory: ./packages/exchange-binance
      - run: npm version 0.1.${{ github.run_number }} --no-git-tag-version
        working-directory: ./packages/adapter/oanda
      - run: npm publish
        working-directory: ./packages/adapter/oanda
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  xtb:
    runs-on: ubuntu-latest
    needs: core
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
          registry-url: 'https://npm.pkg.github.com'

      - run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.QUANTFORM_SECRET }}" >> .npmrc
        working-directory: ./packages/adapter/xtb
      - run: npm install @quantform/core@0.1.${{ github.run_number }}
        working-directory: ./packages/adapter/xtb
      - run: npm install
        working-directory: ./packages/adapter/xtb
      #- run: npm run test:unit
      #  working-directory: ./packages/exchange-binance
      - run: npm version 0.1.${{ github.run_number }} --no-git-tag-version
        working-directory: ./packages/adapter/xtb
      - run: npm publish
        working-directory: ./packages/adapter/xtb
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
